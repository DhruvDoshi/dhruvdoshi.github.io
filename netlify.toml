[build]
  base = "/"
  publish = "build"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
  NPM_VERSION = "10"
  # Netlify sets CI=true; avoid failing the build on CRA warnings.
  CI = "false"

[context.production.environment]
  NODE_VERSION = "22"
